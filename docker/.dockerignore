# .dockerignore - archivos a excluir del contexto de build de Docker (ubicado en docker/)

# Git
.git
.gitignore

# IDEs / editores
.idea/
.vscode/
*.iml

# Dependencias / node / php
node_modules/
vendor/

# Secrets / env
.env
*.pem
*.key

# Sistema / temporales / logs
.DS_Store
Thumbs.db
tmp/
temp/
*.log
*.cache
*.swp
*~

# Build artefacts
dist/
build/

# No ignoramos Dockerfile ni docker-compose.yml aquí (están en este mismo directorio y el build los necesita)
